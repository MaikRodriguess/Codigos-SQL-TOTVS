-- Vê qual é o perfil de acesso do professor

SELECT
	CODPROF,
	CODPESSOA,
	CODIGO, 
	PPESSOA.NOME,
	GUSRPERFIL.CODPERFIL

FROM  SPROFESSOR (NOLOCK)
	LEFT JOIN PPESSOA (NOLOCK)
		ON SPROFESSOR.CODPESSOA = PPESSOA.CODIGO
	LEFT JOIN GUSUARIO (NOLOCK)
		ON PPESSOA.CODUSUARIO = GUSUARIO.CODUSUARIO
	LEFT JOIN GUSRPERFIL (NOLOCK)
		ON GUSUARIO.CODUSUARIO = GUSRPERFIL.CODUSUARIO
WHERE GUSRPERFIL.CODUSUARIO = 'MAIK.SOUZA'

select * from sp_help sprofessor -- CODPESSOA INT, CODPROF
select * from GUSUARIO -- CODUSUARIO VARCHAR, NOME
select * from GUSRPERFIL
WHERE CODUSUARIO = 'vinicius.oliveira'-- CODUSUARIO, CODSISTEMA, CODPERFIL
select * from PPESSOA -- NOME, codusuario, codpessoa